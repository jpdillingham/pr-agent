const expect = require('chai').expect

// const main = require('../src/main').main
// const ReportBuilder = require('../src/classes/reportBuilder')

describe('The main module', function () {
  it('outputs the report', function () {
    // Arrange
    // const reportBuilderMock = this.sandbox.mock(ReportBuilder)

    // Act
    // main()

    // Assert
    // reportBuilderMock.verify()
    // reportBuilderMock.restore()
  })
})